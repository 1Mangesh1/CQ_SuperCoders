<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Invalid Registration</title>
        <style>
            body {
                font-family: Arial, sans-serif;
                background-color: #f2f2f2;
            }
            .container {
                margin: 0 auto;
                max-width: 600px;
                text-align: center;
                padding: 20px;
                background-color: #fff;
                border-radius: 5px;
                box-shadow: 0 0 10px rgba(0,0,0,0.2);
            }
            h1 {
                color: #ff0000;
            }
            p {
                font-size: 18px;
            }
            .timer {
                font-size: 24px;
                margin-top: 20px;
            }
        </style>
    </head>
    <body>
        <div class="container">
            <h1>Email Already Exists</h1>
            <p>Please try again with a different email address.</p>
            <p class="timer">You will be redirected shortly...</p>
        </div>
        <script>
            var count = 5; // set the countdown time in seconds
            var timer = setInterval(function() {
                count--;
                document.querySelector('.timer').innerHTML = 'You will be redirected shortly in ' + count + ' seconds...';
                if (count === 0) {
                    clearInterval(timer);
                    window.location.href = "/register";
                }
            }, 1000); // update the timer every second
        </script>
    </body>
</html>
